#include <iostream>
#include <cmath>
using namespace std;
int main(){
	double x,y;
	cout << "Vvedite znacheniye peremennoy x: ";
	cin >> x;
	y = 3*pow(x,6)-6*pow(x,2)-7;
	cout << "Znacheniye funksii ravno " << y << "." << endl;
	return 0;
}
